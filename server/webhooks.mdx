---
title: "Webhooks"
description: "Receive job results as they happen instead of polling the API."
---

Configure webhooks in the [Filecheck admin](https://admin.filecheck.io) to push results to your server without polling.

## Events

| Event | Fires when |
| --- | --- |
| `job.created` | A job is created. |
| `job.completed` | A job reaches a terminal status. |

## Handling a webhook

On `job.completed`, fetch the full job with your secret key and download `files[n].outputRef`:

```bash
curl https://api.filecheck.io/jobs/{jobId}?expand=runs \
  -H "Authorization: Bearer sk_live_…"
```

<Note>
  Respond `2xx` quickly and do any heavy work asynchronously.
</Note>

<Warning>
  **Signature verification — coming soon.** Verify each webhook's signature before trusting it. The signing scheme and full payload shape are being finalized.

  {/* TODO(filecheck): document the signature header, signing/HMAC scheme, and the webhook payload shape once finalized. */}
  **TODO(filecheck): publish the signing scheme (header + algorithm) and the webhook payload shape.**
</Warning>
